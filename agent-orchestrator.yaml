dataDir: ~/.agent-orchestrator
worktreeDir: ~/.worktrees
port: 3000
defaults:
  runtime: tmux
  agent: claude-code
  workspace: worktree
  notifiers:
    - desktop
projects:
  claude-demo:
    repo: vickybiswas/claude-demo
    path: /Users/vicky/rani/claude-demo
    defaultBranch: main
